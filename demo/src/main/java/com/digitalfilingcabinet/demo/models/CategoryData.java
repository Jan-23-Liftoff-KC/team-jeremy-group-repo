package com.digitalfilingcabinet.demo.models;

public class CategoryData {
    // this one does not expand the abstract class

    // the entire category list. we just make a new object that has all of them.
    // like, the CategoryList class is to make brand new categories
    // this one is just to have an object containing all of those brand new categories
    // so we can do stuff with them, search, etc.

}
